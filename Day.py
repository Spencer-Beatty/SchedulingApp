from Activity import Activity

class Day:
    def __init__(self, dayOfWeek):
        self.dayOfWeek = dayOfWeek
        self.activities = [] # these will be Activity

    def addActivity(self, newActivity):
        high = len(self.activities)
        low = 0
        while(high!=low):
            if(self.activities[high].)

    def addFixedActivity()
